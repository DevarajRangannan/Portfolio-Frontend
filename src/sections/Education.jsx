import React from 'react'
import { CONTAINER, md_CONTAINER } from './styles/Education'

export default function Education() {
  return (
    <div id='education' className={`${CONTAINER} ${md_CONTAINER}`}>
        Education
    </div>
  )
}
