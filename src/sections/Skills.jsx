import React from 'react'
import { CONTAINER, md_CONTAINER } from './styles/Skills'

export default function Skills() {
  return (
    <div id='skills' className={`${CONTAINER} ${md_CONTAINER}`}>
        Skills
    </div>
  )
}
