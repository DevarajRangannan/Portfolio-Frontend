import React from 'react'
import { CONTAINER, md_CONTAINER } from './styles/Contact'

export default function Contact() {
  return (
    <div id='contact' className={`${CONTAINER} ${md_CONTAINER}`}>
        Contact
    </div>
  )
}
